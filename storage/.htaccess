# Deny all direct web access to this folder — temporary uploaded files must
# never be downloadable. Read/written only by server-side PHP.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
